# Do not install unattended updates while Briclite has a durable request for an
# active codec link.  Package-list downloads continue through apt-daily.service.
[Unit]
ConditionPathExists=!/var/lib/briclite/desired-link.json
